<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CashRegistor" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- Change ActionBar background color -->
        <item name="colorPrimary">@android:color/holo_blue_dark</item> <!-- ActionBar background -->
        <item name="colorPrimaryDark">@android:color/holo_blue_dark</item> <!-- StatusBar color -->
        <item name="android:textColorPrimary">@android:color/black</item> <!-- Title text color -->
    </style>


    <style name="Theme.CashRegistor" parent="Base.Theme.CashRegistor" />
</resources>